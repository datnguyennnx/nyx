# Project Agent Notes

This project migrated OpenCode agent and skill prompts into Cursor rules under `.cursor/rules`.

Guidelines:
- Prefer using rule files for task-specific behavior.
- Use documentation-oriented rules for markdown and docs work.
- Use review-oriented rules before finalizing non-trivial changes.
